spring:
  # Test Database Configuration
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

# MQTT Configuration for tests
mqtt:
  broker:
    url: tcp://localhost:1883
  client:
    id: testClient
  topic: test/topic 